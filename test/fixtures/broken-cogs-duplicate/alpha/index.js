// Fixture: claims /dupe. Paired with ../beta, which claims it too.
export default {
    name: "alpha",
    description: "fixture",
    commands: [{ data: { name: "dupe" }, execute() {} }],
    events: [],
};
